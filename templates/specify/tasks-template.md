# Tasks — {{FEATURE}}

> Single source of truth for progress. The orchestrator advances by checking these off. Each task is
> small and verifiable. `[ ]` pending · `[~]` in progress · `[x]` done.

- [ ] T1: _<task>_
- [ ] T2: _<task>_

## Done when

- [ ] All Gherkin scenarios in `contracts/` have passing tests
- [ ] `/verify` is green (build + test + lint + typecheck)
- [ ] `/review` findings resolved
- [ ] Docs synced (ARCHITECTURE / memory / this feature)
