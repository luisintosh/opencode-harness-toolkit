# Memory index

> Grep index of `docs/memory/`. **Not auto-loaded** — agents grep here (and the one-fact files /
> `log.md`) on demand via `/recall`. Promote durable, always-relevant facts into `AGENTS.md` via
> `/distill`. One fact per file: `docs/memory/<slug>.md`.

| Fact | File | Added |
|------|------|-------|
| _(none yet)_ | | |
